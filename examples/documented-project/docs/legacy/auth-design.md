# Authentication design (2024)

Status: Superseded

This document described cookie-based sessions with refresh tokens stored in
AsyncStorage. It conflicts with the current architecture document: sessions
are now JWT-based and refresh tokens live in the platform keychain.

Kept for historical context only.
